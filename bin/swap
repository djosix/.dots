#!/bin/bash -e

temp="$(mktemp -u)"
[[ $# != 2 ]] && {
    echo 'invalid arguments'
    exit 1
}
if [[ -e $1 && -e $2 ]]; then
    echo "swapping files $1 <-> $2"
    mv -v "$1" "$temp"
    mv -v "$2" "$1"
    mv -v "$temp" "$2"
else
    echo "cannot swap files $1 and $2"
    exit 1
fi

